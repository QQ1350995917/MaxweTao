<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@mipmap/splash_bg">

    <ImageView
        android:id="@+id/iv_act_splash_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@mipmap/ic_taomami" />

    <TextView
        android:id="@+id/tv_act_splash_welcome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_act_splash_icon"
        android:gravity="center"
        android:text="@string/app_name"
        android:textSize="28dp" />

    <TextView
        android:id="@+id/tv_act_splash_version"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_act_splash_welcome"
        android:gravity="center"
        android:text="@string/app_name"
        android:textSize="28dp" />

</RelativeLayout>